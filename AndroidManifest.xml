<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.phoenix.osteoporosis"
        >

    <uses-sdk android:minSdkVersion="14"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
            android:icon="@drawable/icon"
            android:label="@string/app_name"
            android:theme="@style/theme"
            >
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity"/>
        <activity android:name=".activities.FakeActivity"/>
        <activity android:name=".activities.AlaemActivity"/>
        <activity android:name=".activities.DarmanActivity"/>
        <activity android:name=".activities.PishgiriActivity"/>
        <activity android:name=".activities.DalaielActivity"/>
        <activity android:name=".activities.ConsultationActivity"/>
        <activity android:name=".activities.SearchActivity"/>
    </application>
</manifest>
